spring:
  application:
    name: service-gateway
  profiles:
    active: dev
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
        # 本地微服务都运行在同一台机器，避免 Wi-Fi 切换后向 Nacos 注册失效的旧网卡 IP。
        ip: ${NACOS_DISCOVERY_IP:127.0.0.1}
